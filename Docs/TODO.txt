Resource:
- Hot reload: file watching (`ResourceHotReload`), reverse-edge dirty propagation, in-place pump swaps | Edit a PNG, see it in-game without restart
- Resource Pipeline: cooked/packed assets, GUID cross-references + rename redirects | Shippable Steam build reads packs, not loose files
- Editor layer: `GetSourcePath`, `ResourceTypeInfo` registry, `Save()` in concept,

Render:
- handle/pool DOD RHI, device owning context CREATION, RenderStats overlay, multi-view, camera-view system, full RHI-only DrawSprite, Vulkan device.

Misc:
- Skill Code Style.
- OpaaxString: Make ctor with num version or static convert to OpaaxString
- continue json format for custom type
- FileSystem for window